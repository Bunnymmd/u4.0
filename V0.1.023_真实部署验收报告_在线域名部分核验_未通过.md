# V0.1.023 真实部署验收报告（已取得线上域名）

**验收对象（只读）：** `微信_第2步收口修正_严格setText_降级状态稳定_V0.1.023_根目录部署包(3).zip`  
**验收域名：** `https://twooo-bz2.pages.dev/`  
**验收日期：** 2026-07-07  
**ZIP 处理：** 未修改、未解包写回、未重打包。  
**结论状态：** **未通过／未完成。仅完成部分真实线上可达性核验，未取得真机启动验收所需原始证据。**

---

## 1. 已取得的真实线上证据

### 1.1 公开 HTTPS 根地址可达

- 真实访问地址：`https://twooo-bz2.pages.dev/`
- 线上抓取结果：成功返回 HTML 内容。
- 抓取结果中的页面标题：`虚拟手机桌面｜WeChat 好友与聊天 · v0.1.023`
- 返回内容包含完整应用界面文字与多个页面的结构性内容，并非部署平台 404 / 默认占位页。

### 1.2 部署平台判断

- 域名为 `*.pages.dev`。
- `pages.dev` 是 Cloudflare Pages 为 Pages 项目提供的默认子域名形式。
- 因本次没有 Cloudflare 项目控制台、构建记录或部署日志，**部署平台只能记录为“由域名推断为 Cloudflare Pages，未获得控制台级确认”**。
- 实际部署时间：**未提供，无法独立取得。**

### 1.3 目标原包身份（离线只读校验）

- ZIP SHA-256：  
  `ebfc58abe10fb5891c3fb8012b040f406dd10c9f12201329f63b28b2b2d3bd7e`
- ZIP 根目录平铺，无额外父目录。
- 原包 `index.html` 中：
  - `data-build="0.1.023"`
  - app JS 目标文件：`./app.446cb1548c1c.js`
  - app JS SRI：  
    `sha384-tlRxjr8T6vW0JSehwoxTv4XGDtc6q6g2MRIclejzR9HCcjc8uvrsf8PgcuDBFq3f`
- 原包内 `app.446cb1548c1c.js` 的重新计算 SHA-384 与上述 SRI 一致。

> 上述仅证明**待验收 ZIP**的预期内容，不能替代线上实际资源响应证明。

---

## 2. 已确认与未确认的边界

| 验收项 | 状态 | 本次原始证据／限制 |
|---|---|---|
| HTTPS 根地址可访问 | 已确认 | 线上地址可打开并返回 HTML 内容。 |
| 页面标题显示 v0.1.023 | 已确认 | 线上抓取结果显示对应标题。 |
| 部署平台 | 部分确认 | `*.pages.dev` 仅支持“Cloudflare Pages 推断”，无控制台日志。 |
| 实际线上 `index.html` 含 `data-build="0.1.023"` | 未确认 | 当前可用线上抓取结果不暴露 HTML 属性原文。 |
| 实际线上 app JS 文件名为 `app.446cb1548c1c.js` | 未确认 | 未取得 Network 的实际请求条目。 |
| 实际线上 app JS SRI 与指定值一致 | 未确认 | 未取得线上 `index.html` 原始 script 标签。 |
| 实际线上 app JS 哈希与 ZIP 一致 | 未确认 | 未取得线上 app JS 原始响应体。 |
| HTML / app JS / release boot 响应头 | 未确认 | 未取得 Network / cURL 原始响应头。 |
| 无自动跳转、重复导航或循环刷新 | 未确认 | 未取得浏览器 Navigation / Network 记录。 |
| 无完整灰屏 | 未确认 | 无真实移动设备截图或录屏。 |
| 无左上角图标飞入、堆叠或重叠 | 未确认 | 无真实移动设备启动录屏。 |
| Dock 不遮挡桌面 | 未确认 | 无真机视觉证据。 |
| 最终状态为 `desktop-positioned` | 未确认 | 未取得页面运行时 DOM dataset。 |
| Worker controller / scope / script URL / registration | 未确认 | 未取得浏览器 Application / Service Worker 原始数据。 |
| Console error / warning | 未确认 | 未取得真实浏览器 Console 导出。 |
| IndexedDB：正常、空库、失败降级 | 未执行 | 无可操作的真实浏览器与储存状态环境。 |
| 刷新三次、关闭重开、强制刷新、旧缓存环境 | 未执行 | 无可操作的真实浏览器与旧缓存 / Worker 环境。 |
| 竖屏、横屏、互切、地址栏收放、分屏 | 未执行 | 无真实移动设备会话与录屏。 |

---

## 3. 不能据此作出的结论

本次不得声称：

- “第 3 步通过”；
- “已确认线上 HTML 为 `data-build="0.1.023"`”；
- “已确认线上加载 `app.446cb1548c1c.js` 与目标 SRI”；
- “已确认无灰屏、无图标飞入、无 Dock 遮挡”；
- “已确认 Worker 已清退且不会循环刷新”；
- “已确认启动状态为 `desktop-positioned`”；
- “已完成真实设备与旧缓存环境验收”。

原因不是发现了失败，而是上述每项都尚未获得规定的**真实浏览器原始证据**。

---

## 4. 本步唯一有效结论

**V0.1.023 已具备可公开访问的 HTTPS 根地址，且线上页面标题表现为 v0.1.023。**

但其余硬性验收条件——实际资源版本、SRI、响应头、Worker 控制状态、启动报告、真机视觉表现、旧缓存与 IndexedDB 边界、横竖屏与地址栏变化——均未完成证据采集。

因此：

> **真实部署验收尚未完成；不得进入下一步；不得生成新版本；不得修改、重打包或变更资源哈希。**

---

## 5. 继续本验收时必须取得的原始证据

必须从至少一台真实移动设备浏览器导出或录制以下项目：

```js
window.__TWO_BOOT_REPORT__
document.documentElement.dataset.twoBootState
document.getElementById("MergedPhone").dataset.startupState
navigator.serviceWorker.controller
await navigator.serviceWorker.getRegistrations()
```

同时保存：

1. 设备型号、系统版本、浏览器与版本；
2. 每个场景的截图或录屏；
3. Network 中 HTML、`app.446cb1548c1c.js`、`release-boot.36a51dff45c7.js` 的请求、状态、响应头与响应来源；
4. Worker script URL、scope、controller、registrations；
5. Console error / warning；
6. 首次打开、连续刷新 3 次、关页重开、旧缓存环境、强制刷新、横竖屏切换、地址栏收放及分屏／窗口尺寸变化后的同类记录。

未取得以上证据前，本报告状态保持：**未通过／未完成。**
